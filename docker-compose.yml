services:
  bot:
    build:
      context: .
    volumes:
      - .:/code
      - sqlite_data:/code/db
    ports:
      - "8001:8000"
    command: |
      bash -c "
        python3 manage.py migrate & 
        python3 manage.py runserver 0.0.0.0:8001 & 
        wait
      "

volumes:
  sqlite_data: