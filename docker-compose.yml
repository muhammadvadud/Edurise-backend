services:
  bot:
    build:
      context: .
    volumes:
      - .:/code
      - sqlite_data:/code/db
    ports:
      - "8088:8000"
    command: |
      bash -c "
        python3 manage.py runserver 0.0.0.0:8088 & 
        wait
      "

volumes:
  sqlite_data: